// import type { Token, TokenizerExtension } from 'marked';

// range(src: string) {
//   const match = /^:::range\s+(\w+)[\s\S]+?:::/m.exec(src);
//   if (match) {
//     return {
//       type: "range",
//       raw: match[0],
//       key: match[1],
//       tokens: [/*tokens*/]
//     }
//   }
// }
